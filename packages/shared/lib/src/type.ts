/** Helper function to simply assert that the value is of the specified type */
export function assertType<T>(_value: T): void {
	// does nothing
}
/** Helper function to simply assert that the value is of the type never */
export function assertNever(_value: never): void {
	// does nothing
}
